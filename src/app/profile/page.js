import ProfileView from "@/components/account/ProfileView";

export const metadata = {
  title: "My Profile",
  description: "Manage your details, addresses and password.",
};

export default function ProfilePage() {
  return <ProfileView />;
}
