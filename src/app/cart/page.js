import CartView from "@/components/cart/CartView";

export const metadata = {
  title: "Your Cart",
  description: "Review your Kiran Sudha cart before checkout.",
};

export default function CartPage() {
  return <CartView />;
}
